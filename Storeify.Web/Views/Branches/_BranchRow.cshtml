@model BranchFormViewModels
<tr>
    <td>@Model.Location</td>
    <td>
        @{

            var statusClass = Model.IsDeleted ? "badge badge-light-danger" : "badge badge-light-success";
            var statusText = Model.IsDeleted ? "Deleted" : "Available";
           
        }
        <span class="badge @statusClass js-status">@statusText</span>
    </td>
    <th>@Model.Phone</th>
    <th>
        @if (Model.Store != null)
        {
            <p>@Model.Store.Name</p>
        }
    </th>
    <th>@Model.CreatedBy</th>
    <th>@Model.CreatedDate</th>
    @* <th>@Model.UpdatedBy</th> *@
    <th class ="js-updated-on"> @Model.UpdatedDate</th>
    @* <th>@Model.DeletedBy</th> *@
    @* <th class ="js-updated-on"> @Model.DeletedDate</th> *@
    @* <th>@Model.DeletedReason</th> *@
    <td>
            <button type="button" class="btn btn-sm btn-icon btn-color-primary btn-active-light-primary"
                data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end" data-kt-menu-overflow="true">
                <span class="svg-icon svg-icon-2">
                    <svg width="24px" height="24px" viewBox="0 0 24 24">
                        <g stroke="none" stroke-width="1" fill="none" fill-rule="evenodd">
                            <rect x="5" y="5" width="5" height="5" rx="1" fill="currentColor"></rect>
                            <rect x="14" y="5" width="5" height="5" rx="1" fill="currentColor" opacity="0.3"></rect>
                            <rect x="5" y="14" width="5" height="5" rx="1" fill="currentColor" opacity="0.3"></rect>
                            <rect x="14" y="14" width="5" height="5" rx="1" fill="currentColor" opacity="0.3"></rect>
                        </g>
                    </svg>
                </span>
            </button>
            <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-800 menu-state-bg-light-primary fw-semibold w-200px py-3"
                 data-kt-menu="true" style="z-index: 107; position: absolute; right: 0; top: 100%;">
                <div class="menu-item px-3 ">
                    <a href="javascript:;" class="menu-link px-3 js-render-modal"
                   data-title="Edit Category" data-url="/Branches/Edit/@Model.Id"
                       data-update = "true">
                    Edit
                </a>
                </div>
                <div class="menu-item px-3">
                    <a href="javascript:;" class="menu-link flex-stack px-3 js-toggle-status"
                   data-id="@Model.Id" data-url="/Branches/ToggleStatus/@Model.Id">
                        Toggle Status
                    </a>
                </div>
            </div>
    </td>
</tr>
